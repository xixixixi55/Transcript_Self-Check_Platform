{
  "name": "@biji/backend",
  "version": "0.1.0",
  "private": true,
  "description": "笔录自检平台后端 (Python FastAPI)",
  "scripts": {
    "dev": "uvicorn app.main:app --reload --host 0.0.0.0 --port 30010",
    "test": "cd app && python -m pytest ../../../tests/ -v",
    "typecheck": "echo 'Python: run mypy for type checking (configured in verify.sh)'"
  }
}

# Windows 便携候选人工验收

## 托盘启动生命周期

- [ ] 在 Windows 10 x64 与 Windows 11 x64 各启动一次候选包，确认启动过程没有命令行窗口或要求一直保留的模态窗口。
- [ ] 确认通知区域显示文枢图标，图标在 100%、125%、150% 显示缩放下轮廓清晰且没有不透明底色。
- [ ] 关闭浏览器窗口后确认文枢后端继续运行；双击托盘图标和右键“打开文枢”均能重新打开当前应用。
- [ ] 右键选择“退出文枢”，确认托盘图标消失、后端结束且再次双击 `文枢.exe` 可以正常启动。
- [ ] 运行期间再次双击 `文枢.exe`，确认不会启动第二个后端实例。
- [ ] 通过任务管理器结束后端进程，确认托盘图标自动撤销并显示明确的停止提示。

## 记录

manual_acceptance: PENDING

候选版本、系统版本、显示缩放、执行人和结果在目标电脑验收后补充；未完成前不得将本变更重新冻结或标记为正式发布通过。

### 2026-08-20 开发机增量证据

- 启动器、托盘状态机与发布校验定向测试：25 passed。
- 原生 Win32 托盘烟雾：托盘创建成功，模拟后端停止后返回 `backend_stopped` 并撤销图标。
- PyInstaller 6.21.0 launcher 构建：成功，日志确认 `Copying icon to EXE`。
- `npm run verify:quick`：通过；架构、类型、治理文档和仓库资产检查均通过。

以上证据不能替代目标电脑上的托盘视觉、交互和进程生命周期人工验收。

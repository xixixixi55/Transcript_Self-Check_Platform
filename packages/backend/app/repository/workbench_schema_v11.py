"""SQLite statements for the v10 to v11 persistence foundation migration."""

from __future__ import annotations


V11_MIGRATION: tuple[str, ...] = (
    # Rebuild the FK parents without disabling foreign_keys. Existing rows are
    # copied before any legacy table is dropped; no cleanup occurs here.
    "ALTER TABLE case_shells RENAME TO case_shells_v10",
    "CREATE TABLE case_shells (case_id TEXT PRIMARY KEY, schema_version INTEGER NOT NULL, case_number TEXT, case_name TEXT NOT NULL, case_summary TEXT NOT NULL, source_id TEXT, parse_task_id TEXT, lifecycle TEXT NOT NULL, report_available INTEGER NOT NULL CHECK(report_available IN (0, 1)), revision INTEGER NOT NULL, created_at TEXT NOT NULL, updated_at TEXT NOT NULL, deployment_instance_id TEXT, record_cleaned INTEGER NOT NULL DEFAULT 0 CHECK(record_cleaned IN (0, 1)), tombstone_revision INTEGER NOT NULL DEFAULT 0, retention_state TEXT NOT NULL DEFAULT 'unknown', cleanup_state TEXT NOT NULL DEFAULT 'not_started', cleaned_at TEXT, last_meaningful_mutation_at TEXT, retention_anchor_utc TEXT, safe_display_summary TEXT, cleanup_revision INTEGER NOT NULL DEFAULT 0)",
    "INSERT INTO case_shells(case_id,schema_version,case_number,case_name,case_summary,source_id,parse_task_id,lifecycle,report_available,revision,created_at,updated_at,record_cleaned,tombstone_revision,retention_state,cleanup_state,cleaned_at,last_meaningful_mutation_at,retention_anchor_utc,safe_display_summary,cleanup_revision) SELECT case_id,schema_version,case_number,case_name,case_summary,source_id,parse_task_id,lifecycle,report_available,revision,created_at,updated_at,0,0,'unknown','not_started',NULL,NULL,NULL,NULL,0 FROM case_shells_v10",
    "ALTER TABLE task_records RENAME TO task_records_v10",
    "CREATE TABLE task_records (task_id TEXT PRIMARY KEY, schema_version INTEGER NOT NULL, case_id TEXT NOT NULL REFERENCES case_shells(case_id), kind TEXT NOT NULL, status TEXT NOT NULL, stage TEXT NOT NULL, percent REAL, counters_json TEXT NOT NULL, input_revision INTEGER NOT NULL, attempt INTEGER NOT NULL, process_binding_json TEXT, error_code TEXT, error_summary TEXT, cancel_requested INTEGER NOT NULL CHECK(cancel_requested IN (0, 1)), created_at TEXT NOT NULL, started_at TEXT, updated_at TEXT, finished_at TEXT, progress_kind TEXT, stage_label TEXT, stage_index INTEGER, stage_count INTEGER, last_heartbeat_at TEXT, output_bytes INTEGER, output_volume_count INTEGER, last_output_change_at TEXT, worker_state TEXT, allowed_actions_json TEXT NOT NULL DEFAULT '[]', revision INTEGER NOT NULL, deployment_instance_id TEXT, publication_id TEXT, word_artifact_id TEXT, formal_verified_at TEXT)",
    "INSERT INTO task_records(task_id,schema_version,case_id,kind,status,stage,percent,counters_json,input_revision,attempt,process_binding_json,error_code,error_summary,cancel_requested,created_at,started_at,updated_at,finished_at,progress_kind,stage_label,stage_index,stage_count,last_heartbeat_at,output_bytes,output_volume_count,last_output_change_at,worker_state,allowed_actions_json,revision,deployment_instance_id,publication_id,word_artifact_id,formal_verified_at) SELECT task_id,schema_version,case_id,kind,status,stage,percent,counters_json,input_revision,attempt,process_binding_json,error_code,error_summary,cancel_requested,created_at,started_at,updated_at,finished_at,progress_kind,stage_label,stage_index,stage_count,last_heartbeat_at,output_bytes,output_volume_count,last_output_change_at,worker_state,allowed_actions_json,revision,deployment_instance_id,NULL,NULL,NULL FROM task_records_v10",
    "ALTER TABLE source_records RENAME TO source_records_v10",
    "CREATE TABLE source_records (source_id TEXT PRIMARY KEY, schema_version INTEGER NOT NULL, case_id TEXT NOT NULL REFERENCES case_shells(case_id), task_id TEXT REFERENCES task_records(task_id), source_type TEXT, internal_path TEXT, allowed_root TEXT, allowed_root_id TEXT, metadata_json TEXT, fingerprint_json TEXT, access_status TEXT NOT NULL, requires_reselection INTEGER NOT NULL CHECK(requires_reselection IN (0, 1)), revalidation_error_code TEXT, last_verified_at TEXT, revision INTEGER NOT NULL, created_at TEXT NOT NULL, updated_at TEXT NOT NULL, deployment_instance_id TEXT, tombstone_state TEXT NOT NULL DEFAULT 'active', tombstoned_at TEXT, tombstone_revision INTEGER NOT NULL DEFAULT 0)",
    "INSERT INTO source_records(source_id,schema_version,case_id,task_id,source_type,internal_path,allowed_root,allowed_root_id,metadata_json,fingerprint_json,access_status,requires_reselection,revalidation_error_code,last_verified_at,revision,created_at,updated_at,tombstone_state,tombstoned_at,tombstone_revision) SELECT source_id,schema_version,case_id,task_id,source_type,internal_path,allowed_root,allowed_root_id,metadata_json,fingerprint_json,access_status,requires_reselection,revalidation_error_code,last_verified_at,revision,created_at,updated_at,'active',NULL,0 FROM source_records_v10",
    "ALTER TABLE archive_attempts RENAME TO archive_attempts_v10",
    "CREATE TABLE archive_attempts (attempt_id TEXT PRIMARY KEY, schema_version INTEGER NOT NULL, case_id TEXT NOT NULL REFERENCES case_shells(case_id), task_id TEXT, deployment_instance_id TEXT, source_id TEXT NOT NULL REFERENCES source_records(source_id), input_revision INTEGER NOT NULL, source_revision INTEGER, draft_revision INTEGER, report_fingerprint TEXT, status TEXT NOT NULL CHECK(status IN ('accepted', 'running', 'succeeded', 'failed', 'interrupted')), cleanup_status TEXT NOT NULL CHECK(cleanup_status IN ('not_required', 'pending', 'succeeded', 'failed', 'unknown')), error_code TEXT, manifest_id TEXT, manifest_source_key TEXT, manifest_input_fingerprint TEXT, manifest_archive_fingerprint TEXT, input_snapshot_id TEXT, input_snapshot_root_id TEXT, input_snapshot_locator TEXT, input_snapshot_fingerprint TEXT, input_snapshot_status TEXT, staging_root_id TEXT, staging_locator TEXT, ownership_marker_token TEXT, process_pid INTEGER, process_started_at TEXT, created_at TEXT NOT NULL, started_at TEXT, finished_at TEXT, revision INTEGER NOT NULL)",
    "INSERT INTO archive_attempts(attempt_id,schema_version,case_id,task_id,deployment_instance_id,source_id,input_revision,source_revision,draft_revision,report_fingerprint,status,cleanup_status,error_code,manifest_id,manifest_source_key,manifest_input_fingerprint,manifest_archive_fingerprint,input_snapshot_id,input_snapshot_root_id,input_snapshot_locator,input_snapshot_fingerprint,input_snapshot_status,staging_root_id,staging_locator,ownership_marker_token,process_pid,process_started_at,created_at,started_at,finished_at,revision) SELECT attempt_id,schema_version,case_id,task_id,deployment_instance_id,source_id,input_revision,source_revision,draft_revision,report_fingerprint,status,cleanup_status,error_code,manifest_id,manifest_source_key,manifest_input_fingerprint,manifest_archive_fingerprint,input_snapshot_id,input_snapshot_root_id,input_snapshot_locator,input_snapshot_fingerprint,input_snapshot_status,staging_root_id,staging_locator,ownership_marker_token,process_pid,process_started_at,created_at,started_at,finished_at,revision FROM archive_attempts_v10",
    "ALTER TABLE archive_context_bindings RENAME TO archive_context_bindings_v10",
    "CREATE TABLE archive_context_bindings (context_hash TEXT PRIMARY KEY, attempt_id TEXT NOT NULL REFERENCES archive_attempts(attempt_id), case_id TEXT NOT NULL REFERENCES case_shells(case_id), source_id TEXT, source_revision INTEGER, draft_revision INTEGER, report_fingerprint TEXT, context_kind TEXT NOT NULL DEFAULT 'workbench', active INTEGER NOT NULL CHECK(active IN (0, 1)), expires_at TEXT, consumed_at TEXT, created_at TEXT NOT NULL)",
    "INSERT INTO archive_context_bindings(context_hash,attempt_id,case_id,source_id,source_revision,draft_revision,report_fingerprint,context_kind,active,expires_at,consumed_at,created_at) SELECT context_hash,attempt_id,case_id,source_id,source_revision,draft_revision,report_fingerprint,context_kind,active,expires_at,consumed_at,created_at FROM archive_context_bindings_v10",
    "ALTER TABLE archive_publish_intents RENAME TO archive_publish_intents_v10",
    "CREATE TABLE archive_publish_intents (intent_id TEXT PRIMARY KEY, attempt_id TEXT NOT NULL UNIQUE REFERENCES archive_attempts(attempt_id), task_id TEXT, deployment_instance_id TEXT, case_id TEXT NOT NULL REFERENCES case_shells(case_id), source_id TEXT NOT NULL REFERENCES source_records(source_id), source_revision INTEGER NOT NULL, draft_revision INTEGER NOT NULL, report_fingerprint TEXT NOT NULL, source_key TEXT NOT NULL, input_fingerprint TEXT NOT NULL, archive_fingerprint TEXT NOT NULL, manifest_id TEXT NOT NULL, relative_final_dir TEXT NOT NULL, public_manifest_json TEXT NOT NULL, publication_id TEXT, publication_relative_dir TEXT, publication_digest TEXT, publication_file_set_json TEXT, publication_status TEXT, fence_id TEXT, phase TEXT NOT NULL CHECK(phase IN ('intent_persisted', 'published', 'indexed', 'verified', 'conflict')), publication_verified_at TEXT, created_at TEXT NOT NULL, updated_at TEXT NOT NULL)",
    "INSERT INTO archive_publish_intents(intent_id,attempt_id,task_id,deployment_instance_id,case_id,source_id,source_revision,draft_revision,report_fingerprint,source_key,input_fingerprint,archive_fingerprint,manifest_id,relative_final_dir,public_manifest_json,publication_id,publication_relative_dir,publication_digest,publication_file_set_json,publication_status,fence_id,phase,publication_verified_at,created_at,updated_at) SELECT intent_id,attempt_id,task_id,deployment_instance_id,case_id,source_id,source_revision,draft_revision,report_fingerprint,source_key,input_fingerprint,archive_fingerprint,manifest_id,relative_final_dir,public_manifest_json,publication_id,publication_relative_dir,publication_digest,publication_file_set_json,publication_status,fence_id,phase,NULL,created_at,updated_at FROM archive_publish_intents_v10",
    "ALTER TABLE archive_publish_fences RENAME TO archive_publish_fences_v10",
    "CREATE TABLE archive_publish_fences (fence_id TEXT PRIMARY KEY, attempt_id TEXT NOT NULL UNIQUE REFERENCES archive_attempts(attempt_id), task_id TEXT, deployment_instance_id TEXT, case_id TEXT NOT NULL REFERENCES case_shells(case_id), source_id TEXT NOT NULL REFERENCES source_records(source_id), source_revision INTEGER NOT NULL, draft_revision INTEGER NOT NULL, report_fingerprint TEXT NOT NULL, context_hash TEXT NOT NULL, shell_revision INTEGER NOT NULL, status TEXT NOT NULL CHECK(status IN ('active', 'pending_verification', 'consumed', 'released', 'invalidated')), reason TEXT, created_at TEXT NOT NULL, updated_at TEXT NOT NULL)",
    "INSERT INTO archive_publish_fences(fence_id,attempt_id,task_id,deployment_instance_id,case_id,source_id,source_revision,draft_revision,report_fingerprint,context_hash,shell_revision,status,reason,created_at,updated_at) SELECT fence_id,attempt_id,task_id,deployment_instance_id,case_id,source_id,source_revision,draft_revision,report_fingerprint,context_hash,shell_revision,status,reason,created_at,updated_at FROM archive_publish_fences_v10",
    "ALTER TABLE archive_input_snapshots RENAME TO archive_input_snapshots_v10",
    "CREATE TABLE archive_input_snapshots (snapshot_id TEXT PRIMARY KEY, task_id TEXT NOT NULL, attempt_id TEXT NOT NULL UNIQUE REFERENCES archive_attempts(attempt_id), deployment_instance_id TEXT NOT NULL, case_id TEXT NOT NULL REFERENCES case_shells(case_id), source_id TEXT NOT NULL REFERENCES source_records(source_id), source_revision INTEGER NOT NULL, draft_revision INTEGER NOT NULL, source_root_id TEXT NOT NULL, snapshot_root_id TEXT NOT NULL, snapshot_locator TEXT NOT NULL, manifest_json TEXT NOT NULL, input_fingerprint TEXT NOT NULL, status TEXT NOT NULL CHECK(status IN ('copying', 'sealed', 'invalidated', 'cleaned')), marker_token TEXT NOT NULL, created_at TEXT NOT NULL, sealed_at TEXT, updated_at TEXT NOT NULL)",
    "INSERT INTO archive_input_snapshots(snapshot_id,task_id,attempt_id,deployment_instance_id,case_id,source_id,source_revision,draft_revision,source_root_id,snapshot_root_id,snapshot_locator,manifest_json,input_fingerprint,status,marker_token,created_at,sealed_at,updated_at) SELECT snapshot_id,task_id,attempt_id,deployment_instance_id,case_id,source_id,source_revision,draft_revision,source_root_id,snapshot_root_id,snapshot_locator,manifest_json,input_fingerprint,status,marker_token,created_at,sealed_at,updated_at FROM archive_input_snapshots_v10",
    "ALTER TABLE case_drafts RENAME TO case_drafts_v10",
    "CREATE TABLE case_drafts (case_id TEXT PRIMARY KEY REFERENCES case_shells(case_id) ON DELETE CASCADE, schema_version INTEGER NOT NULL, report_json TEXT NOT NULL, report_version TEXT NOT NULL, field_states_json TEXT NOT NULL, asset_refs_json TEXT NOT NULL, template_ref_json TEXT, archive_plan_id TEXT, lifecycle TEXT NOT NULL, revision INTEGER NOT NULL, created_at TEXT NOT NULL, updated_at TEXT NOT NULL)",
    "INSERT INTO case_drafts SELECT * FROM case_drafts_v10",
    "ALTER TABLE edit_leases RENAME TO edit_leases_v10",
    "CREATE TABLE edit_leases (lease_id TEXT PRIMARY KEY, schema_version INTEGER NOT NULL, case_id TEXT NOT NULL REFERENCES case_shells(case_id) ON DELETE CASCADE, session_id TEXT NOT NULL, client_instance_id TEXT NOT NULL, lease_token TEXT NOT NULL, last_heartbeat_at TEXT NOT NULL, expires_at TEXT NOT NULL, status TEXT NOT NULL, takeover_of_lease_id TEXT, revision INTEGER NOT NULL)",
    "INSERT INTO edit_leases SELECT * FROM edit_leases_v10",
    "ALTER TABLE asset_references RENAME TO asset_references_v10",
    "CREATE TABLE asset_references (asset_id TEXT PRIMARY KEY, case_id TEXT NOT NULL REFERENCES case_shells(case_id) ON DELETE CASCADE, asset_kind TEXT NOT NULL, fingerprint TEXT, metadata_json TEXT NOT NULL, status TEXT NOT NULL, created_at TEXT NOT NULL)",
    "INSERT INTO asset_references SELECT * FROM asset_references_v10",
    "ALTER TABLE archive_plans RENAME TO archive_plans_v10",
    "CREATE TABLE archive_plans (plan_id TEXT PRIMARY KEY, schema_version INTEGER NOT NULL, case_id TEXT NOT NULL REFERENCES case_shells(case_id), plan_revision INTEGER NOT NULL, input_inventory_revision INTEGER NOT NULL, mapping_revision INTEGER NOT NULL, volume_slots_json TEXT NOT NULL, verified_slots_json TEXT NOT NULL DEFAULT '[]', created_at TEXT NOT NULL, updated_at TEXT NOT NULL, revision INTEGER NOT NULL)",
    "INSERT INTO archive_plans SELECT * FROM archive_plans_v10",
    # Drop only copied legacy tables after all dependent rows are safe in the
    # replacement tables. Formal publication rows and files are untouched.
    "DROP TABLE archive_context_bindings_v10",
    "DROP TABLE archive_publish_intents_v10",
    "DROP TABLE archive_publish_fences_v10",
    "DROP TABLE archive_input_snapshots_v10",
    "DROP TABLE archive_attempts_v10",
    "DROP TABLE source_records_v10",
    "DROP TABLE task_records_v10",
    "DROP TABLE case_drafts_v10",
    "DROP TABLE edit_leases_v10",
    "DROP TABLE asset_references_v10",
    "DROP TABLE archive_plans_v10",
    "DROP TABLE case_shells_v10",
    "CREATE TABLE case_retention_policies (deployment_instance_id TEXT PRIMARY KEY, mode TEXT NOT NULL DEFAULT 'disabled' CHECK(mode IN ('disabled', 'preview_only', 'enforce')), retention_days INTEGER NOT NULL CHECK(retention_days BETWEEN 1 AND 3650), scan_interval_seconds INTEGER NOT NULL CHECK(scan_interval_seconds >= 3600), batch_size INTEGER NOT NULL CHECK(batch_size BETWEEN 1 AND 1000), policy_revision INTEGER NOT NULL, activated_at TEXT, created_at TEXT NOT NULL, updated_at TEXT NOT NULL)",
    "CREATE TABLE case_retention_records (retention_record_id TEXT PRIMARY KEY, deployment_instance_id TEXT NOT NULL, case_id TEXT NOT NULL REFERENCES case_shells(case_id), eligibility TEXT NOT NULL CHECK(eligibility IN ('eligible', 'ineligible', 'unknown')), status TEXT NOT NULL CHECK(status IN ('unknown', 'not_expired', 'eligible', 'blocked', 'planned', 'processing', 'completed', 'failed')), last_meaningful_mutation_at TEXT, latest_verified_formal_publication_at TEXT, latest_successful_word_export_at TEXT, retention_anchor_utc TEXT, expires_at_utc TEXT, last_blocker_code TEXT, policy_revision INTEGER NOT NULL, case_revision INTEGER NOT NULL, cleanup_revision INTEGER NOT NULL DEFAULT 0, created_at TEXT NOT NULL, updated_at TEXT NOT NULL, UNIQUE(deployment_instance_id, case_id))",
    "CREATE TABLE case_cleanup_runs (cleanup_run_id TEXT PRIMARY KEY, deployment_instance_id TEXT NOT NULL, case_id TEXT NOT NULL REFERENCES case_shells(case_id), policy_revision INTEGER NOT NULL, case_revision_at_plan INTEGER NOT NULL, case_revision_at_claim INTEGER, owner_instance_id TEXT, claim_token TEXT, lease_expires_at TEXT, fence_epoch INTEGER, current_phase TEXT NOT NULL CHECK(current_phase IN ('planned', 'claimed', 'preflighted', 'work_files_cleaned', 'records_cleaned', 'verified', 'succeeded', 'blocked', 'stale', 'cancel_requested', 'cancelled', 'interrupted', 'partial_failure', 'failed_retryable', 'failed_terminal')), retry_count INTEGER NOT NULL DEFAULT 0, file_step_result TEXT, result_code TEXT, error_code TEXT, created_at TEXT NOT NULL, updated_at TEXT NOT NULL, completed_at TEXT)",
    "CREATE TABLE formal_word_artifacts (word_artifact_id TEXT PRIMARY KEY, deployment_instance_id TEXT NOT NULL, case_id TEXT NOT NULL REFERENCES case_shells(case_id), publication_id TEXT NOT NULL, internal_relative_path TEXT NOT NULL, file_digest TEXT NOT NULL, file_size INTEGER NOT NULL CHECK(file_size >= 0), source_manifest_digest TEXT NOT NULL, template_identity TEXT NOT NULL, template_version TEXT NOT NULL, generated_at TEXT NOT NULL, verified_at TEXT, status TEXT NOT NULL CHECK(status IN ('pending', 'verified', 'invalid')), created_at TEXT NOT NULL, updated_at TEXT NOT NULL, UNIQUE(deployment_instance_id, word_artifact_id))",
    "CREATE UNIQUE INDEX active_case_lease ON edit_leases(case_id) WHERE status = 'active'",
    "CREATE INDEX task_case_status ON task_records(case_id, status)",
    "CREATE INDEX source_case ON source_records(case_id)",
    "CREATE INDEX source_deployment_state ON source_records(deployment_instance_id, tombstone_state, case_id)",
    "CREATE INDEX archive_attempt_case_status ON archive_attempts(case_id, status)",
    "CREATE INDEX archive_context_attempt ON archive_context_bindings(attempt_id, active)",
    "CREATE INDEX archive_publish_attempt ON archive_publish_intents(attempt_id, phase)",
    "CREATE UNIQUE INDEX archive_publish_fence_active_case ON archive_publish_fences(case_id) WHERE status = 'active'",
    "CREATE UNIQUE INDEX archive_publish_fence_active_attempt ON archive_publish_fences(attempt_id) WHERE status = 'active'",
    "CREATE INDEX archive_publish_fence_reconciliation ON archive_publish_fences(status, attempt_id)",
    "CREATE INDEX archive_plan_case_revision ON archive_plans(case_id, plan_revision DESC)",
    "CREATE INDEX archive_task_current ON task_records(case_id, kind, status, updated_at DESC)",
    "CREATE INDEX archive_task_deployment_status ON task_records(deployment_instance_id, kind, status, updated_at DESC)",
    "CREATE INDEX archive_attempt_task ON archive_attempts(task_id, deployment_instance_id, status)",
    "CREATE INDEX archive_snapshot_attempt ON archive_input_snapshots(attempt_id, status)",
    "CREATE INDEX archive_snapshot_owner ON archive_input_snapshots(task_id, deployment_instance_id, status)",
    "CREATE INDEX archive_publish_intent_task ON archive_publish_intents(task_id, deployment_instance_id, phase)",
    "CREATE INDEX archive_publish_fence_task ON archive_publish_fences(task_id, deployment_instance_id, status)",
    "CREATE INDEX archive_attempt_deployment_status ON archive_attempts(deployment_instance_id, status, created_at)",
    "CREATE INDEX archive_publication_verified ON archive_publish_intents(deployment_instance_id, case_id, publication_verified_at)",
    "CREATE UNIQUE INDEX case_retention_case ON case_retention_records(deployment_instance_id, case_id)",
    "CREATE UNIQUE INDEX cleanup_run_active_case ON case_cleanup_runs(deployment_instance_id, case_id) WHERE current_phase IN ('planned', 'claimed', 'preflighted', 'work_files_cleaned', 'records_cleaned', 'verified', 'interrupted', 'partial_failure', 'failed_retryable', 'cancel_requested')",
    "CREATE INDEX cleanup_run_recoverable ON case_cleanup_runs(deployment_instance_id, current_phase, updated_at)",
    "CREATE INDEX cleanup_run_lease ON case_cleanup_runs(deployment_instance_id, lease_expires_at)",
    "CREATE INDEX cleanup_run_deployment_scan ON case_cleanup_runs(deployment_instance_id, current_phase, created_at)",
    "CREATE INDEX formal_word_case ON formal_word_artifacts(deployment_instance_id, case_id, generated_at DESC)",
    "CREATE INDEX formal_word_publication ON formal_word_artifacts(deployment_instance_id, publication_id, status)",
)
